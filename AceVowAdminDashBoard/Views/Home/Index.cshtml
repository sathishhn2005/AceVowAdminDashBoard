

<div class="page-content">
    <!-- Page title -->
    <div class="page-title">
        <div class="row justify-content-between align-items-center">
            <div class="col-md-6 d-flex align-items-center justify-content-between justify-content-md-start mb-3 mb-md-0">
                <!-- Page title + Go Back button -->
                <div class="d-inline-block">
                    <h5 class="h4 d-inline-block font-weight-400 mb-0 text-white">Client List</h5>
                </div>
                <!-- Additional info -->
                <div class="align-items-center ml-4 d-inline-flex">
                    <span class="h4 text-info mb-0 mr-2">9</span>
                    <span class="text-sm opacity-7 text-white">New products</span>
                </div>
                <!-- Optional link -->
                <a href="card-listing.html" class="text-sm text-info d-none d-lg-inline-block ml-4">
                    See
                    cards
                </a>
            </div>
            <div class="col-md-6 d-flex align-items-center justify-content-between justify-content-md-end">
                <div class="actions actions-dark d-inline-block">
                    <a href="#" class="action-item ml-md-4">
                        <i class="far fa-file-export mr-2"></i>Export
                    </a>
                    <a href="#" class="action-item ml-3">
                        <i class="far fa-cog mr-2"></i>Settings
                    </a>
                </div>
                <a href="#" class="btn btn-sm btn-white btn-icon-only rounded-circle ml-4">
                    <span class="btn-inner--icon"><i class="far fa-plus"></i></span>
                </a>
            </div>
        </div>
    </div>
    <!-- Listing -->
    <div class="card">
        <!-- Card header -->
        <div class="card-header actions-toolbar border-0">
            <div class="actions-search" id="actions-search">
                <div class="input-group input-group-merge input-group-flush">
                    <div class="input-group-prepend">
                        <span class="input-group-text bg-transparent"><i class="far fa-search"></i></span>
                    </div>
                    <input type="text" class="form-control form-control-flush"
                           placeholder="Type and hit enter ...">
                    <div class="input-group-append">
                        <a href="#" class="input-group-text bg-transparent" data-action="search-close"
                           data-target="#actions-search"><i class="far fa-times"></i></a>
                    </div>
                </div>
            </div>
            <div class="row justify-content-between align-items-center">
                <div class="col">
                    <h6 class="d-inline-block mb-0">Client List</h6>
                </div>
                <div class="col text-right">
                    <div class="actions">
                        <a href="#" class="action-item mr-3" data-action="search-open"
                           data-target="#actions-search"><i class="far fa-search"></i></a>
                        <div class="dropdown mr-3">
                            <a href="#" class="action-item" role="button" data-toggle="dropdown"
                               aria-haspopup="true" aria-expanded="false">
                                <i class="far fa-filter"></i>
                            </a>
                            <div class="dropdown-menu dropdown-menu-right">
                                <a class="dropdown-item" href="#">
                                    <i class="far fa-sort-amount-down"></i>Newest
                                </a>
                                <a class="dropdown-item" href="#">
                                    <i class="far fa-sort-alpha-down"></i>From A-Z
                                </a>
                                <a class="dropdown-item" href="#">
                                    <i class="far fa-sort-alpha-up"></i>From Z-A
                                </a>
                            </div>
                        </div>
                        <div class="dropdown" data-toggle="dropdown">
                            <a href="#" class="action-item" role="button" data-toggle="dropdown"
                               aria-haspopup="true" aria-expanded="false">
                                <i class="far fa-ellipsis-h"></i>
                            </a>
                            <div class="dropdown-menu dropdown-menu-right">
                                <a href="#" class="dropdown-item">Refresh</a>
                                <a href="#" class="dropdown-item">Manage Widgets</a>
                                <a href="#" class="dropdown-item">Settings</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <!-- Table -->
        <h1 class="text-center text-success">@TempData["UserMess"]</h1>
        
        <div class="table-responsive">
            <table id="tblClient" class="table align-items-center">
                <thead>
                    <tr>
                        <th>StoreName</th>
                        <th>Contact Name</th>
                        <th>Email Id</th>
                        <th>Address</th>

                        <th>BulkUpload</th>
                        <th>Add Category</th>
                        <th>Edit</th>
                        <th>Active/Deactive</th>
                    </tr>
                </thead>
                <tbody class="list">
                </tbody>
            </table>
        </div>
    </div>
    <!-- Load more -->
    <div class="mt-4 text-center">
        <a href="#" class="btn btn-sm btn-neutral rounded-pill shadow hover-translate-y-n3">
            Load more
            ...
        </a>
    </div>
</div>
<div class="d-none p-t-30">

    <div class="row">
      
        <div class="col-md-12">
            <div class="box d-none">
                <div class="box-header">
                    <h4>Client Search</h4>
                    <div class="row">

                        <div class="form-group col-sm-3 col-xs-6 mb-0">
                            @Html.TextBox("txtClientName", "", new { @class = "form-control w-full", @placeholder = "Enter Client Name" })
                        </div>
                        <div class="form-group col-sm-3 col-xs-6 mb-0">
                            @Html.TextBox("txtStoreUrl", "", new { @class = "form-control w-full", @placeholder = "Enter Store Url" })
                        </div>
                        <div class="form-group col-sm-3 col-xs-6 mb-0">
                            @Html.TextBox("txtClientCity", "", new { @class = "form-control w-full", @placeholder = "Enter City" })
                        </div>
                        <div class="form-group col-sm-3 col-xs-6 mb-0">
                            <button class="btn btn-primary btn-sm" id="btnSearch" type="button">Search</button>
                        </div>
                    </div>
                </div>
            </div>
            
            <div class="box">
                <!-- /.box-header -->
                <h1 class="text-center text-success">@TempData["Alertmsg"]</h1>

                <div class="box-body">
                    <!-- Nav tabs -->
                    <ul class="nav nav-tabs" role="tablist">
                        <li class="nav-item"> <a class="nav-link active" data-toggle="tab" href="#home8" role="tab"><span><i class="mdi mdi-hotel mr-10"></i>Client List</span></a> </li>
                        <li class="d-none nav-item"> <a class="nav-link" data-toggle="tab" href="#profile8" role="tab"><span class="noti-dot"><i class="mdi mdi-checkbox-marked-circle-outline mr-10"></i>Recent Approvals</span></a> </li>
                    </ul>


                    <!-- Tab panes -->
                    <div class="tab-content tabcontent-border">
                        <div class="tab-pane active" id="home8" role="tabpanel">
                            <div class="tab-pane table-responsive active" id="All">
                                <table id="tblClient" class="table m-b-0 table-hover">
                                    <thead class="text-center">
                                        <tr>
                                            <th>StoreName</th>
                                            <th>Contact Name</th>
                                            <th>Email Id</th>
                                            <th>Address</th>

                                            <th>BulkUpload</th>
                                            <th>Add Category</th>
                                            <th>Edit</th>
                                            <th>Active/Deactive</th>

                                        </tr>
                                    </thead>
                                    <tbody class="text-center">
                                    </tbody>
                                </table>
                            </div>
                        </div>
                        
                    </div>
                </div>
                <!-- /.box-body -->
            </div>
            <!-- /.box -->
            <!-- /.col -->
        </div>
    </div>
</div>
<div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">AceVow</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <input type="hidden" id="hdnId" name="hdnId" value="0" />
                <form>
                    <div class="form-group">
                        <label for="recipient-name" class="col-form-label">From Date:</label>
                        <input type="date" class="form-control" name="FromDate" id="txtFromDate">
                    </div>
                    <div class="form-group">
                        <label for="message-text" class="col-form-label">To Date:</label>
                        <input type="date" class="form-control" name="ToDate" id="txtToDate">
                    </div>
                    <div class="form-group">
                        <label for="message-text" id="lblCount" class="text-success col-form-label"></label>

                    </div>

                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                <button type="button" id="btnGetCount" onclick="qrCount()" class="btn btn-primary">Get Count</button>
            </div>
        </div>
    </div>
</div>
@using (Html.BeginForm("BulkUpdate", "Home", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    <div id="uploadBulk" class="modal fade bs-example-modal-lg" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title mt-0" id="uploadBulkLabel">Upload Bulk Policies</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body text-center">
                    <h2>Bulk File Upload for Client</h2>
                    @*<p class="lead">Number of Files Attached<b> 5</b></p>*@

                    <!-- Upload  -->
                <div id="file-upload-form" class="uploader">
                    <input id="fileUploadRIMaster" type="file" name="CSVFile" accept=".csv" />

                    @*name="CSVFile" id="fileUploadRIMaster" accept=".csv"*@
                    <label for="fileUploadRIMaster" id="file-drag">
                        <img id="file-image" src="#" alt="Preview" class="hidden">
                        <div id="start">
                            <i class="fa fa-download" aria-hidden="true"></i>
                            <div>Select a file or drag here</div>
                            <div id="notimage" class="hidden">Please select an image</div>
                            <span id="file-upload-btn" class="btn btn-primary">Select a file</span>
                        </div>
                        <div id="response" class="hidden">
                            <div id="messages"></div>
                            <progress class="progress" id="file-progress" value="0">
                                <span>0</span>%
                            </progress>
                        </div>
                    </label>
                </div>
                </div>
            </div>
            <!-- /.modal-content -->
        </div>
        <!-- /.modal-dialog -->
    </div>
}

<div id="CategoryUpdate" class="modal fade bs-example-modal-lg" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-md">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title mt-0" id="uploadBulkLabel">Add Category</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>

            <div class="modal-body">
                <form id="formCat">
                    <h1 class="text-center text-success">@TempData["CatMess"]</h1>
                    <div class="form-group">
                        <label for="txtParentCatName" class="col-form-label">Parent Category</label>
                        <input type="text" class="form-control" id="txtParentCatName">
                        @*@Html.TextBox("txtParentCatName", "", new { @class = "form-control w-full", @placeholder = "Enter  Name" })*@
                    </div>
                    <div class="form-group">
                        <label for="txtCatName" class="col-form-label">Category Name</label>
                        <input type="text" class="form-control" id="txtCatName">
                    </div>
                    <div class="form-group">
                        <input type="submit" class="btn btn-primary" id="btnCat">
                    </div>
                </form>
            </div>


        </div>
        <!-- /.modal-content -->
    </div>
    <!-- /.modal-dialog -->
</div>


<script>

    $(document).ready(function () {
        GetClient();
        // window.location.href = "/Home/EditClient";
    });

    $('#btnSearch').on('click', function () {
        GetClient();
    });
    function GetClient() {
        
        var param = { ClientName: $('#txtClientName').val(), StoreUrl: $('#txtStoreUrl').val(), City: $('#txtClientCity').val() };
        $("#tblClient").find("tr:not(:first)").remove();

        $.ajax({

            type: "GET",
            url: "GetClientInfo",
            dataType: "json",
            contentType: 'application/x-www-form-urlencoded; charset=UTF-8',
            data: param,
            success: function (Result) {
                var checked = false;
                if (Result.length > 0) {
                    var i = 0;
                    $.each(Result, function (key, value) {
                        i++;
                        var hdnID = "hdnobjJSonVal_" + i;
                        var hdId = i;
                        var JVal = JSON.stringify(value);

                        $('#tblClient').append('<tr> <td><a href="javascript:qrCount(' + value.Id + ');">' + value.StoreName + '</a></td><td>' + value.ContactName + '</td><td>' + value.EmailId + '</td><td>' + value.AddressLine1 + '</td><td><button class="btn btn-primary btn-sm" type="button" data-toggle="modal" onclick=SetId(' + value.Id + ') data-target="#uploadBulk"><i class="fas fa-cloud-upload-alt"></i></button></td><td><button class="btn btn-secondary-login" type="button" data-toggle="modal" onclick=SetId(' + value.UserId + ') data-target="#CategoryUpdate"><i class="fas fa-layer-group"></i></button></td><td class="text-center"><i class="fas fa-pencil-alt text-primary mx-3" onclick="EditClientMaster(' + "'" + value.Id + "'" + ')"></i></td><td><div class="custom-control custom-switch"><input type="checkbox" class="custom-control-input" id=' + hdnID + ' onchange="Deactivate(' + value.UserId + ',' + value.IsActive + ')" value = ' + value.UserId + ' > <label class="custom-control-label" for=' + hdnID + '></label> </div ><input type="hidden" id=' + hdnID + ' value=' + "'" + JVal + "'" + '></td></tr>');
                        if (value.IsActive == 0) {
                            $("#" + hdnID).prop('checked', false);
                        }
                        else {
                            $("#" + hdnID).prop('checked', true);
                        }

                    })
                }
                else {
                    $('#tblClient').append('<tr> <td colspan="14" aligen="center" > No Records found </td></tr>');
                }

            },
            error: function () {
                // $("#lblMessage").html('Error while Registring the Company!.');
            }
        })
    }
    
    function Deactivate(id, isactive) {
        var hdnID = "hdnobjJSonVal_" + id;
        var isactive;
        if ($('input:checkbox[id=' + hdnID+']').is(':checked') == true) {
            isactive = true;
        }
        else { isactive = false; };
        var UId = '' + id;
        var objUser = { UserId: UId, IsActive: isactive };
        $.ajax({
            type: "POST",
            url: "DeactivateClient",
            dataType: "json",
            contentType: 'application/x-www-form-urlencoded; charset=UTF-8',
            data: objUser,
            success: function (Result) {
                if (Result == "DeActivated") {
                    //$(obj).closest('tr').remove();
                    alert("DeActivated successfully");
                    GetClient();
                    //return false;
                }
                else { alert(Result); }
            },
            error: function () {
                console.log('Error while Registring the Company!.');
            }
        })
    }

    function DeActiveClient(obj, Uid) {
       
        

    }
    function SetId(id) {
        $('#hdnId').val(id);

    }
    function csvFileUpload() {

        var rowCount = 0;
        var regex = /^([a-zA-Z0-9\s_\\.\-:])+(.csv|.txt)$/;
        if (regex.test($("#fileUploadRIMaster").val().toLowerCase())) {

            if (typeof (FileReader) != "undefined") {
                var reader = new FileReader();
                reader.onload = function (e) {

                    var rows = e.target.result.split('\r\n');

                    for (var i = 0; i < rows.length; i++) {

                        var cells = rows[i].split(",");
                        var RRC = rowCount + 1;
                        if (i == 0) {


                            // Heater validation
                            if (cells.length == 9) {

                                if (cells[0] != "Name") {
                                    $('#fileUploadRIMaster').val('');
                                    alert("Please check column name mismatch (Name).");
                                    return false;
                                }
                                if (cells[1] != "Category") {
                                    $('#fileUploadRIMaster').val('');
                                    alert("Please check column name mismatch (Category).");
                                    return false;
                                }
                                //if (cells[2] != "Description") {
                                //    $('#fileUploadRIMaster').val('');
                                //    alert("Please check column name mismatch (Description).");
                                //    return false;
                                //}
                                if (cells[3] != "RegularPrice") {
                                    $('#fileUploadRIMaster').val('');
                                    alert("Please check column name mismatch (RegularPrice).");
                                    return false;
                                }
                                if (cells[4] != "TaxType") {
                                    $('#fileUploadRIMaster').val('');
                                    alert("Please check column name mismatch (TaxType).");
                                    return false;
                                }
                                //if (cells[5] != "IsRecommended") {
                                //    $('#fileUploadRIMaster').val('');
                                //    alert("Please check column name mismatch (IsRecommended).");
                                //    return false;
                                //}
                                if (cells[6] != "IsActive") {
                                    $('#fileUploadRIMaster').val('');
                                    alert("Please check column name mismatch (IsActive).");
                                    return false;
                                }
                                if (cells[7] != "Email") {
                                    $('#fileUploadRIMaster').val('');
                                    alert("Please check column name mismatch (Email).");
                                    return false;
                                }
                                if (cells[8] != "Image") {
                                    $('#fileUploadRIMaster').val('');
                                    alert("Please check column name mismatch (Image).");
                                    return false;
                                }
                            }
                            else {
                                $('#fileUploadRIMaster').val('');
                                alert("Upload Csv file invalid template , Please check .");
                                return false;
                            }

                        } else {
                            // Row validation


                            if (cells.length == 9) {
                                if (cells[0] == "") {
                                    $('#fileUploadRIMaster').val('');
                                    alert("Please check Row NO : " + RRC + " Name  is empty");
                                    return false;
                                }
                                if (cells[1] == "") {
                                    $('#fileUploadRIMaster').val('');
                                    alert("Please check Row NO : " + RRC + " Category is empty");
                                    return false;
                                }

                                //if (cells[2] == "") {
                                //    $('#fileUploadRIMaster').val('');
                                //    alert("Please check Row NO : " + RRC + " Description  is empty");
                                //    return false;
                                //}

                                if (cells[3] == "") {
                                    $('#fileUploadRIMaster').val('');
                                    alert("Please check Row NO : " + RRC + " RegularPrice is empty");
                                    return false;
                                }

                                //if (cells[4] == "") {
                                //    $('#fileUploadRIMaster').val('');
                                //    alert("Please check Row NO : " + RRC + " TaxType is empty");
                                //    return false;
                                //}
                                //if (cells[5] == "") {
                                //    $('#fileUploadRIMaster').val('');
                                //    alert("Please check Row NO : " + RRC + " IsRecommended is empty");
                                //    return false;
                                //}

                                if (cells[6] == "") {
                                    $('#fileUploadRIMaster').val('');
                                    alert("Please check Row NO : " + RRC + " IsActive is empty");
                                    return false;
                                }
                                if (cells[7] == "") {
                                    $('#fileUploadRIMaster').val('');
                                    alert("Please check Row NO : " + RRC + " Email is empty");
                                    return false;
                                }
                                if (cells[8] == "") {
                                    $('#fileUploadRIMaster').val('');
                                    alert("Please check Row NO : " + RRC + " Image is empty");
                                    return false;
                                }

                            }
                            else {

                                if (cells.length > 1) {
                                    $('#fileUploadRIMaster').val('');
                                    alert("Please check Row NO : " + RRC + " This row have comma simple or invalied Column length");
                                    return false;
                                }
                            }

                        }

                        rowCount += 1;

                    }

                    if (rowCount == rows.length) {
                        $("#hdnMsgStatus").val('Y');

                        $("#uploadFiles").click();

                    }

                }
                reader.readAsText($("#fileUploadRIMaster")[0].files[0]);
            } else {
                $('#fileUploadRIMaster').val('');
                alert("This browser does not support HTML5.");
                return false;
            }
        } else {
            $('#fileUploadRIMaster').val('');
            alert("Please upload a valid CSV file.");
            return false;
        }
    }

    $(document).ready(function () {
        $("#txtParentCatName").autocomplete({
            minLength: 2,
            autoFocus: true,
            open: function () { $($('.ui-autocomplete')).css('width', '300px'); },
            source: function (request, response) {
                var param = { prefixText: $('#txtParentCatName').val(), Action: "ParentCatName" };

                $.ajax({
                    url: "/Home/GetParentCateogryName",
                    data: JSON.stringify(param),
                    dataType: "json",
                    type: "POST",
                    contentType: "application/json; charset=utf-8",

                    success: function (data) {
                        response($.map(data, function (item) {
                            return {

                                label: item.Name,
                                value: item.Name
                            }
                        }))
                    },
                    error: function (XMLHttpRequest, textStatus, errorThrown) {
                        var err = eval("(" + XMLHttpRequest.responseText + ")");
                        //alert(err.Message)
                        console.log("Ajax Error! " + err.Message);
                    }
                });
            },

        });
        $("#txtCatName").autocomplete({
            minLength: 2,
            autoFocus: true,
            open: function () { $($('.ui-autocomplete')).css('width', '300px'); },
            source: function (request, response) {
                var param = { prefixText: $('#txtCatName').val(), Action: "ParentCatName" };

                $.ajax({
                    url: "/Home/GetParentCateogryName",
                    data: JSON.stringify(param),
                    dataType: "json",
                    type: "POST",
                    contentType: "application/json; charset=utf-8",

                    success: function (data) {
                        response($.map(data, function (item) {
                            return {

                                label: item.Name,
                                value: item.Name
                            }
                        }))
                    },
                    error: function (XMLHttpRequest, textStatus, errorThrown) {
                        var err = eval("(" + XMLHttpRequest.responseText + ")");
                        //alert(err.Message)
                        console.log("Ajax Error! " + err.Message);
                    }
                });
            },

        });
        $('#btnCat').on('click', function () {

            if ($.trim($("#txtCatName").val()) == "") {
                alert("Please enter CatName");
                $("#txtCatName").focus();
                return false;
            }
            if ($.trim($("#txtParentCatName").val()) == "") {
                alert("Please enter ParentCatName");
                $("#txtParentCatName").focus();
                return false;
            }


            var objCat = new Object();
            objCat.UserId = $('#hdnId').val();
            objCat.Name = $("#txtCatName").val();
            objCat.ParentCategory = $("#txtParentCatName").val();

            var param = {
                obj: objCat
            };

            $.ajax({
                type: "POST",
                url: "/Home/AddCategory",
                dataType: "json",
                contentType: 'application/json',
                data: JSON.stringify(param),
                success: function (res) {
                    alert(res);
                    $('#formCat').reset();
                },
                error: function () {
                    alert("Error");
                    $('#formCat').reset();
                }
            })
        });
        //qrCount(0);

    });
    function EditClientMaster(id) {
        // window.location.href = "/Home/EditClient?Id='" + id + "'";
        $('#hdnId').val(id);
        window.location.href = "/Home/EditClient?hdnId=" + id + "";
        //window.open(
        //    "/Home/EditClient?hdnId=" + id + ""// <- This is what makes it open in a new window.
        //);

    }
    function qrCount(id) {
        $('#exampleModal').modal('show');
        //SetId(id);

        if (id == 0 || id == undefined) {
            id = $('#hdnId').val();
        }
        var objUser = { FromDate: $('#txtFromDate').val(), ToDate: $('#txtToDate').val(), UserId: id };
        $.ajax({
            type: "GET",
            contentType: "application/json; charset=utf-8",
            url: "GetCountQR",
            data: objUser,
            dataType: "json",
            success: function (Result) {
                $('#lblCount').html("Total No.of count " + Result.Result);
                $('#hdnId').val(id);
            },
            error: function (Result) {
                alert("Error");
            }
        });

    }
</script>